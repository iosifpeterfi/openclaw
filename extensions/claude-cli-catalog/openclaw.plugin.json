{
  "id": "claude-cli-catalog",
  "activation": {
    "onStartup": false
  },
  "enabledByDefault": true,
  "name": "Claude CLI Catalog",
  "description": "Registers claude-cli/{opus,sonnet,haiku} model entries so cron + sub-agent dispatch resolves to the existing claude-cli runner.",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {}
  }
}
